{
  "description": "Developer Second Brain — after real editing work, silently ask the model to judge whether it is worth recording (proposes only; never saves)",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/detect-on-edit.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
