# macOS
.DS_Store

# Claude Code local session settings
.claude/settings.local.json

# Claude Code worktrees (local scratch copies of the repo)
.claude/worktrees/

# Local vault (in case a user points SECOND_BRAIN_VAULT into the repo)
/vault/
